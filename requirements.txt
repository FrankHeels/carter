# Core
Django>=5.1,<6.0
pillow>=11.0          # работа с изображениями
black

# Database
psycopg2-binary>=2.9  # PostgreSQL (убрать если используешь SQLite)

# Static files
whitenoise>=6.7       # раздача статики без nginx в продакшне

# Environment
python-decouple>=3.8  # .env файлы (SECRET_KEY, DEBUG и т.д.)

# Production server
gunicorn>=22.0
